{
  "name": "send-text",
  "version": "1.0.0",
  "description": "A simple nodejs wrapper for sending text messages with the twilio API",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Patrick Larabee",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^7.0.0",
    "twilio": "^3.30.1"
  }
}
